server:
  port: 8001
eureka:
  client:
    service-url:
      ##注意此处的参数名称不可以写成是defaule-zone，虽然不会报错但是无效参数
      defaultZone: http://localhost:8001/eureka/
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false
